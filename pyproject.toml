[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "fastapi-churn"
version = "0.1.0"
description = "FastAPI service for ML churn prediction"
authors = [
    { name = "Semchenok Vsevolod" }
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.136.3",
    "uvicorn>=0.49.0",
]
